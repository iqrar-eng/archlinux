#!/usr/bin/env bash
# Usage: fd [any fd options/args, e.g. -d 2 "$HOME"]

mapfile -t selected < <(fd -H . "$@" | fzf -m)

[ "${#selected[@]}" -eq 0 ] && exit 0

~/archlinux/.config/yazi/bin/open --current-tab "${selected[@]}"
exit 0
